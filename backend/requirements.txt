# --- web layer -------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- persistence -----------------------------------------------------------
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pgvector==0.3.6

# --- auth / crypto ---------------------------------------------------------
# Password hashing uses stdlib hashlib.scrypt (see app/security.py) rather than
# passlib+bcrypt, which avoids the passlib 1.7.4 / bcrypt 4.x incompatibility.
PyJWT==2.10.1
cryptography==44.0.0

# --- learning --------------------------------------------------------------
numpy>=2.1,<3
scipy>=1.14,<2
scikit-learn>=1.5,<2

# --- LLM layer (optional at runtime; the app degrades gracefully without a key)
anthropic==0.42.0
httpx==0.28.1
